// ==========================================
// Лимиты озвучки — количество СИМВОЛОВ, доступных пользователю в сутки.
// ==========================================
// Вынесены в отдельный файл (было внутри tts.controller.ts), потому что
// billing.service.ts тоже должен их знать — при покупке/апгрейде тарифа
// нужно «доливать» новый дневной лимит поверх уже потраченного (см.
// createSubscription в billing.service.ts).
//
// Подняты примерно вдвое от исходных значений (4000/8000/20000/40000) —
// пользователь пожаловался, что лимитов не хватает на активное
// использование голосового режима и озвучки в чате.
export const TTS_DAILY_LIMITS: Record<string, number> = {
  FREE:  8000,   // было 4000
  PLUS:  16000,  // было 8000
  PRO:   40000,  // было 20000
  ULTRA: 80000,  // было 40000
};

export function todayDayKey(): string {
  return new Date().toISOString().slice(0, 10);
}
